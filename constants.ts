import { CoinData, Order, ChartPoint } from './types';

// Initial empty states or placeholders while loading real data
export const INITIAL_COINS: CoinData[] = [];

export const INITIAL_ORDER_BOOK_ASKS: Order[] = [];
export const INITIAL_ORDER_BOOK_BIDS: Order[] = [];

export const INITIAL_CHART_DATA: ChartPoint[] = [];
